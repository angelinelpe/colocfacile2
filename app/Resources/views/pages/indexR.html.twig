
{{ include('pages/inc/header.html.twig') }}        	

<br>
<a href='{{ absolute_url(asset('')) }}app_dev.php/annonce' class="btn btn-primary btn-lg">Faire une réservation</a>


<br><br>
<h3>Mes réservations</h3>
<br><br>

<table class="table table-hover" style="width:1500px; margin-left:-15%;">
  <thead>
    <tr>
      <th scope="col">Id de la réservation</th>
      <th scope="col">Id de l'annonce</th>
      <th scope="col">Date de la réservation</th>
    </tr>
  </thead>
  <tbody>

    {% for reservation in reservations %}

    <tr>
      <th scope="row">{{reservation.id}}</th>
      <td>{{reservation.idAnnonce}}</td>
      <td>{{reservation.dateReservation|date('Y-m-d')}}</td>
      <td><a href='{{ absolute_url(asset('')) }}app_dev.php/viewR/{{ reservation.id }}' class="btn btn-primary btn-sm">Voir</a>
        <a href="{{ absolute_url(asset('')) }}app_dev.php/deleteR/{{ reservation.id }}" class="btn btn-danger btn-sm">Supprimer</a>

      </td>
    </tr>

  {% else %}
    <tr>
      <td>/NA</td>
    </tr>

  {% endfor %}

  </tbody>
</table> 



{{ include('pages/inc/footer.html.twig') }}        	
